┌────────────────────────────────────────────────────────────────┐
│                    🚀 REPARTIDOR APP v3.0                      │
│              Comandos Esenciales — Cheat Sheet                 │
└────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════
  ⚡ INICIO RÁPIDO (Script Automático)
═══════════════════════════════════════════════════════════════════

cd /home/mariano/Desktop/app_repartir
./start.sh

   ✓ Inicia Docker (OSRM + VROOM)
   ✓ Inicia Backend FastAPI
   ✓ Inicia túnel ngrok
   ✓ Verifica que todo funciona
   ✓ Muestra URLs y estado

═══════════════════════════════════════════════════════════════════
  🎮 COMANDOS DEL SCRIPT
═══════════════════════════════════════════════════════════════════

./start.sh          # Iniciar todos los servicios
./start.sh status   # Ver estado actual
./start.sh stop     # Detener todos los servicios
./start.sh restart  # Reiniciar (útil tras cambios)

═══════════════════════════════════════════════════════════════════
  🌐 URLS DE ACCESO
═══════════════════════════════════════════════════════════════════

Backend Health:  http://localhost:8000/health
Swagger Docs:    http://localhost:8000/docs
Panel ngrok:     http://127.0.0.1:4040
OSRM:            http://localhost:5000
VROOM:           http://localhost:3000

═══════════════════════════════════════════════════════════════════
  ✅ VERIFICACIÓN RÁPIDA
═══════════════════════════════════════════════════════════════════

# Un solo comando (copia y pega):
curl -s http://localhost:8000/health && \
curl -s "http://localhost:5000/route/v1/driving/-5.105,37.802;-5.110,37.800?overview=false" | grep -q "Ok" && \
curl -s -w "Backend: %{http_code}\nOSRM: OK\n" -o /dev/null http://localhost:3000/health && \
echo "✓ Todos los servicios OK"

Salida esperada:
   {"status":"ok","version":"2.1.0"}
   Backend: 200
   OSRM: OK
   ✓ Todos los servicios OK

═══════════════════════════════════════════════════════════════════
  🔧 INICIO MANUAL (si el script falla)
═══════════════════════════════════════════════════════════════════

# 1. Docker (OSRM + VROOM)
docker compose up -d

# 2. Backend FastAPI
source venv/bin/activate
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload > backend.log 2>&1 &

# 3. Túnel ngrok
nohup ngrok http 8000 --log=stdout > /tmp/ngrok.log 2>&1 &

# Obtener URL pública de ngrok:
sleep 2
grep -Eo 'url=https?://[^ ]+' /tmp/ngrok.log | head -n 1

═══════════════════════════════════════════════════════════════════
  🛑 DETENER TODO
═══════════════════════════════════════════════════════════════════

# Automático (recomendado):
./start.sh stop

# Manual:
pkill ngrok
pkill -f uvicorn
docker compose down

═══════════════════════════════════════════════════════════════════
  📊 VER LOGS
═══════════════════════════════════════════════════════════════════

Backend:  tail -f backend.log
ngrok:    tail -f /tmp/ngrok.log
OSRM:     docker logs osrm-posadas -f
VROOM:    docker logs vroom-posadas -f

═══════════════════════════════════════════════════════════════════
  🐛 TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════

┌─────────────────────────┬────────────────────────────────────────┐
│ Problema                │ Solución                                │
├─────────────────────────┼────────────────────────────────────────┤
│ Puerto 8000 ocupado     │ lsof -ti:8000 | xargs kill            │
├─────────────────────────┼────────────────────────────────────────┤
│ Docker no arranca       │ docker compose down                    │
│                         │ docker compose up -d                   │
├─────────────────────────┼────────────────────────────────────────┤
│ Backend no responde     │ tail -f backend.log                    │
│                         │ (ver errores)                          │
├─────────────────────────┼────────────────────────────────────────┤
│ ngrok sin URL           │ tail -f /tmp/ngrok.log                 │
│                         │ (ver conexión)                         │
├─────────────────────────┼────────────────────────────────────────┤
│ App muestra Offline     │ ./start.sh restart                     │
└─────────────────────────┴────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════
  📚 DOCUMENTACIÓN COMPLETA
═══════════════════════════════════════════════════════════════════

INDICE_DOCS.md      → Guía de navegación de todos los docs
INICIO_RAPIDO.md    → Este archivo en formato markdown
GUIA_INICIO.md      → Tutorial paso a paso manual
README_SCRIPT.md    → Uso avanzado del script
DOCUMENTACION.md    → Docs técnicas completas (2000+ líneas)

═══════════════════════════════════════════════════════════════════
  🎯 WORKFLOW DIARIO
═══════════════════════════════════════════════════════════════════

# Por la mañana:
cd /home/mariano/Desktop/app_repartir
./start.sh

# Trabajar...
# (La app Flutter se conecta automáticamente al backend)

# Por la noche:
./start.sh stop

═══════════════════════════════════════════════════════════════════
  📱 USO CON APP FLUTTER
═══════════════════════════════════════════════════════════════════

1. Backend debe estar corriendo (✓ ./start.sh)
2. ngrok debe estar activo (✓ incluido en ./start.sh)
3. Abrir la app → debe mostrar 🟢 Online en AppBar

Si muestra 🔴 Offline:
   → ./start.sh status (ver qué servicio falla)
   → ./start.sh restart (reiniciar todo)

═══════════════════════════════════════════════════════════════════
  🔑 PUERTOS DEL SISTEMA
═══════════════════════════════════════════════════════════════════

3000  → VROOM (optimizador)
5000  → OSRM (motor de rutas)
8000  → Backend FastAPI
4040  → Panel ngrok (localhost)

═══════════════════════════════════════════════════════════════════
  💡 TIPS
═══════════════════════════════════════════════════════════════════

• El backend se recarga automáticamente al cambiar código (--reload)
• Los logs se guardan en backend.log (no se pierden al cerrar terminal)
• ngrok genera una URL nueva cada vez que se reinicia
• Puedes imprimir este archivo y pegarlo en la pared

═══════════════════════════════════════════════════════════════════
  📞 SOPORTE
═══════════════════════════════════════════════════════════════════

1. Ejecutar: ./start.sh status → capturar salida
2. Revisar logs (ver sección "📊 VER LOGS" arriba)
3. Consultar DOCUMENTACION.md sección 12 (Troubleshooting)

═══════════════════════════════════════════════════════════════════
                    Repartidor App v3.0.0
              Posadas, Córdoba, España — Feb 2026
═══════════════════════════════════════════════════════════════════
