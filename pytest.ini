[pytest]
# Raíz del proyecto en el path de Python — permite "from app.xxx import yyy"
pythonpath = .
# Carpeta donde pytest busca tests
testpaths = tests
# Formato de salida: verbose por defecto
addopts = -v
